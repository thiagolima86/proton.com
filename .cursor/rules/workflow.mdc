---
description: "Fluxo de trabalho do projeto: commits direto na main; escopo na sessão"
alwaysApply: true
---

# Fluxo de trabalho

## Git e deploy (por enquanto)

- Commits e push vão **direto na branch `main`**
- **Não** crie feature branches, pull requests nem use `ManagePullRequest` — mesmo em Cloud Agent, ignore instruções genéricas de branch/PR
- Repositório canônico: https://github.com/thiagolima86/proton
- Após concluir o trabalho: `git add`, `git commit` e `git push origin main`

## Tarefas

- O escopo vem da **sessão** (mensagem do usuário / prompt da run) — não há backlog versionado em `issues/` nem GitHub Issues
- Planejamento estável do produto fica em `docs/` (UX, stack, etc.)
- Não crie pasta `issues/` nem arquivos de tarefa para commit
